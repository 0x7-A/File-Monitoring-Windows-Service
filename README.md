# File-Monitoring-Windows-Service
Windows service that automaticlly check if there any file created in source folder, and move it to another folder with renaming to GUID  and deleted from the source folder, logging all the events and errors, folder path cang change from the config
